# Automatically added by dh_pycentral
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall python-ogre
	if grep -qs '^python-ogre$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^python-ogre$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
